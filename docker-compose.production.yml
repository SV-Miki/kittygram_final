volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    image: ${DOCKER_USERNAME}/kittygram_backend:latest
    env_file: .env
    volumes:
      - static:/static
      - media:/app/media
    depends_on:
      - db

  frontend:
    image: ${DOCKER_USERNAME}/kittygram_frontend:latest
    command: sh -c "mkdir -p /static/frontend && cp -r /app/build/. /static/frontend/"
    volumes:
      - static:/static

  gateway:
    image: ${DOCKER_USERNAME}/kittygram_gateway:latest
    ports:
      - 9000:80
    volumes:
      - static:/static
      - media:/media
    depends_on:
      - backend
      - frontend
